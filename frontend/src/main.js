// fleet-app loads the fleetview SPA via the Wails AssetServer's
// reverse-proxy handler, so we never use this stub directly. It only
// exists because Wails's build pipeline expects an entrypoint.
console.log("fleet-app boot");
